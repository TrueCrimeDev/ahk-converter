# Privacy Rules
- NEVER include hardcoded local file paths (e.g., C:\Users\...) in code, commits, comments, or output.
- NEVER reference the usernames "uphol", "upholdjd", or "anondev" anywhere in code, commits, or prompts.
- NEVER reference the GitHub repo "012090120901209" in output or commits.
- Always use relative paths or environment variables instead of absolute local paths.
- All git commits must use the TrueCrimeAudit identity — never the local machine username.

# Remove all of these sub headings / sub titles below:

Basic Information
Authorship

Version Information
Links & References
Classification
Technical Details

# Remove this field description too:
Comma - separated topical terms

# Remove the linebreaks in the webview

# Remove the file name too that's at the top, it's not working out correctly.