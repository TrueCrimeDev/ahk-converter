trigger:
  tags:
    include:
      - 'v*'

pool:
  vmImage: 'ubuntu-latest'

variables:
  - name: nodeVersion
    value: '20.x'

stages:
  - stage: Build
    displayName: 'Build Extension'
    jobs:
      - job: BuildJob
        displayName: 'Build and Package'
        steps:
          - task: NodeTool@0
            displayName: 'Install Node.js'
            inputs:
              versionSpec: '$(nodeVersion)'

          - script: npm ci
            displayName: 'Install dependencies'

          - script: npm run compile
            displayName: 'Compile TypeScript'

          - script: npx vsce package --out extension.vsix
            displayName: 'Package extension'

          - task: PublishPipelineArtifact@1
            displayName: 'Publish VSIX artifact'
            inputs:
              targetPath: '$(Build.SourcesDirectory)/extension.vsix'
              artifactName: 'vsix'

  - stage: Publish
    displayName: 'Publish to Marketplace'
    dependsOn: Build
    condition: and(succeeded(), or(startsWith(variables['Build.SourceBranch'], 'refs/tags/v'), eq(variables['Build.Reason'], 'Manual')))
    jobs:
      - job: PublishJob
        displayName: 'Publish Extension'
        steps:
          - task: DownloadPipelineArtifact@2
            displayName: 'Download VSIX artifact'
            inputs:
              artifact: 'vsix'
              path: '$(Pipeline.Workspace)/vsix'

          - task: NodeTool@0
            displayName: 'Install Node.js'
            inputs:
              versionSpec: '$(nodeVersion)'

          - script: npm install -g @vscode/vsce
            displayName: 'Install vsce globally'

          - script: vsce publish --packagePath $(Pipeline.Workspace)/vsix/extension.vsix --pat $(VSCE_PAT) --skip-duplicate
            displayName: 'Publish to VS Code Marketplace'

